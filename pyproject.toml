[tool.poetry]
name = "vocles"
version = "0.1.0"
description = "Pandas extensions for bioacoustics"
authors = ["David Nicholson <nickledave@users.noreply.github.com>"]
license = "BSD"

[tool.poetry.dependencies]
python = "^3.7.1"
pandas = "^1.2.2"
crowsetta = "^3.1.0"
SoundFile = "^0.10.3"

[tool.poetry.dev-dependencies]
pytest = "^6.2.2"
pytest-cov = "^2.11.1"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
